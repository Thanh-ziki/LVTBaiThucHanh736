@model IEnumerable<MvcMovie.Models.Animal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KhachHang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayBan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KhachHang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayBan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaBan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaBan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaBan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
